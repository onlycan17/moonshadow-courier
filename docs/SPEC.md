# PC·모바일 횡스크롤 액션 RPG 제작 명세와 프롬프트

이 문서는 현재 저장소에 구현된 게임을 처음부터 다시 만들 수 있도록 제품 요구사항, 데이터 규칙,
에셋·오디오 제작법, 검증 기준과 AI 코딩 에이전트용 프롬프트를 한곳에 모은 재현 명세다.
2026-07-29의 P0~P79 구현을 기준으로 하며, 구현 세부의 최종 기준은 이 문서에서 지목하는 순수 규칙과
데이터 모듈이다. 내용이 충돌하면 이 문서의 최종 제품 계약, 현재 소스의 순수 규칙·테스트,
`AGENTS.md`, 과거 `ROADMAP.md`·QA 기록 순으로 판단한다. 과거 단계의 수치는 회귀 이력이지 최종
명세가 아니다.

## 1. 문서 사용법

가장 안정적인 제작 순서는 다음과 같다.

1. 이 문서 전체를 AI 코딩 에이전트의 프로젝트 컨텍스트로 제공한다.
2. `14. 한 번에 사용하는 마스터 프롬프트`를 먼저 전달해 저장소와 `ROADMAP.md`를 만든다.
3. `15. 단계별 실행 프롬프트`를 P0부터 순서대로 하나씩 전달한다.
4. 매 단계가 끝날 때 타입 검사, 단위 테스트, 프로덕션 빌드와 관련 E2E를 통과시킨 뒤 커밋한다.
5. 마지막에 `16. 최종 인수 시나리오`를 실제 PC와 가로·세로 터치 브라우저에서 수행한다.

처음부터 모든 기능을 한 번에 구현하도록 요청하지 않는다. 이동·충돌, 저장, 전투처럼 서로 영향을
크게 주는 기능을 작은 수직 슬라이스로 완성해야 회귀 원인을 추적할 수 있다.

## 2. 제품 목표

독자적인 픽셀 아트 세계에서 표창을 주 무기로 사용하는 캐릭터를 육성하는 로컬 싱글 플레이 웹
액션 RPG 데모를 만든다. 별도 플러그인이나 서버 없이 브라우저에서 실행되며, 로그인부터 Lv.200
최종 보스와 비전투 점프 맵까지 PC 키보드 또는 모바일 터치만으로 플레이할 수 있어야 한다.

핵심 플레이 루프는 세 가지다.

- 입문 루프: `로그인 → 첫 캐릭터 생성 → 캐릭터 선택 → 전직 시험 수락 → 몬스터 5체 처치 → 전직 보고 → 드롭 회수 → 귀환`
- 성장 루프: `사냥 → EXP·메소 획득 → AP·SP 분배 → 장비 구매 → Lv.30·60·120 전직 → 신규 스킬 해금`
- 후반 루프: `Lv.100 원정 수락 → 중간보스 → 상위보스 → Lv.200 최종보스 → 보고`, 또는 `인내의 숲 정상 보상 획득`

완료의 의미는 화면만 비슷한 모형이 아니라 입력, 물리, 전투, 보상, 퀘스트, 저장과 복구가 하나의
끊김 없는 플레이 흐름으로 동작하는 상태다.

## 3. 범위와 기술 기준

| 구분 | 필수 기준 |
| --- | --- |
| 플랫폼 | PC 데스크톱·모바일·태블릿 웹 브라우저 |
| 지원 브라우저 | Windows 최신 Chrome·Edge·Firefox, macOS 최신 Chrome·Firefox·Safari, 최신 터치 브라우저 |
| 제외 | 게임패드·네이티브 앱·멀티플레이·계정 서버 |
| 기술 | TypeScript, Vite, Phaser 3, DOM/CSS, Vitest, Playwright |
| 렌더링 | 1280×720 논리 해상도, 픽셀 아트, nearest-neighbor, 정수 좌표 반올림 |
| 화면 대응 | 종횡비 유지 `FIT`, PC 중앙 정렬·최소 800×600, 터치 전체 창·가로 844×390·세로 390×844 검증, 세로 DOM 화면은 Canvas 위아래까지 `100dvh` 사용 |
| 물리 | Phaser Arcade Physics, 중력 y=1250, fixed step |
| 저장 | 브라우저 origin별 `localStorage`, 최대 3개 독립 슬롯 |
| 네트워크 | 게임 플레이와 런타임 에셋은 외부 네트워크에 의존하지 않음 |
| 성능 | 60 FPS 목표, 자동화 루프 종료 50 FPS 이상, 실제 장치 30분 점검 |

Node.js는 `^20.19.0` 또는 `>=22.12.0`을 사용한다. 기본 명령은 `npm ci`, `npm run dev`,
`npm run typecheck`, `npm test`, `npm run build`, `npm run release:check`로 통일한다.

## 4. 독자성·권리·에셋 원칙

이 프로젝트가 옛 횡스크롤 MMORPG의 화면 밀도와 조작 리듬을 참고하더라도, 타 게임의 코드,
맵 지형, UI 이미지, 음원, 로고, 캐릭터, 몬스터, 아이콘이나 고유 문양을 복제하지 않는다.

- 참고 이미지는 레이아웃, 색감, 화면 점유율과 동작 분석에만 사용한다.
- 배포 에셋은 직접 제작, 생성형 도구로 제작, 또는 배포 권리가 확인된 것만 사용한다.
- 공개 배포본에서는 기존 작품을 직접 떠올리게 하는 직업·기술·보스 고유명도 독자 명칭으로 교체한다.
- 생성형 에셋에는 특정 작품, 캐릭터, 작가나 현존 아티스트를 그대로 모방하라는 지시를 넣지 않는다.
- 생성 원본과 프롬프트·제작 기록은 보존하되 런타임과 `dist/`에는 최적화한 파생본만 포함한다.
- 모든 파일의 제작자, 원본, 변환, 라이선스와 배포 조건을 `ASSET_CREDITS.md`에 기록한다.

이 문서의 한국어 명칭은 현재 데모를 설명하기 위한 기준값이다. 다른 사람이 재사용할 때는 데이터
계층에서 한꺼번에 교체할 수 있도록 표시 문자열과 내부 ID를 분리한다.

## 5. 화면 흐름 명세

### 5.1 Scene 구성

1. `Boot`: 폰트, 화면 배경, 맵, lossless WebP 스프라이트·스킬 아이콘, UI, 일반 BGM·SFX를 프리로드하고 매니페스트를 검증한다. 보스 BGM은 살아 있는 보스가 있는 맵에서 처음 요청한다.
2. `Login`: 아이디와 비밀번호가 비어 있지 않으면 로그인한다. 실제 인증은 하지 않으며 비밀번호를 저장하지 않는다.
3. `CharacterCreate`: 저장 캐릭터가 없거나 빈 슬롯을 선택했을 때 닉네임과 주사위 능력치를 확정한다.
4. `CharacterSelect`: 최대 3개 슬롯의 닉네임·레벨·직업을 비교하고 선택 슬롯의 상세 능력치를 보여 준다.
5. `Gameplay`: 선택 슬롯의 성장, 위치, 퀘스트, 인벤토리와 장비를 불러와 현재 맵에서 시작한다.

첫 접속에는 기본 캐릭터를 자동 생성하지 않는다. 캐릭터 생성 전에는 프로필 키와 활성 슬롯 키도
쓰지 않으며, 프로필 없이 Gameplay에 진입하면 CharacterCreate로 복구한다.

### 5.2 캐릭터 생성

- 닉네임은 앞뒤 공백을 제거하고 NFC로 정규화한다.
- 한글·영문·숫자만 허용하며 Unicode 글자 수 2~12자다.
- STR·DEX·INT·LUK는 각각 4~13, 합계 25다.
- 주사위 재굴림 중에는 재굴림과 생성 버튼을 잠그고 애니메이션이 끝난 값만 저장한다.
- 신규 캐릭터는 Lv.9 초보자, HP 180/180, MP 90/90, 메소 0, 커닝시티 시작이다.
- 생성 화면의 선택 버튼으로 Lv.120 호카게 부스트를 켤 수 있다. 부스트는 자동 능력치 배분,
  HP/MP 완전 회복, 전체 15개 스킬 Lv.20과 남은 SP 33을 선택한 슬롯에만 저장한다.
- 화면 폼 검증과 별개로 실제 저장 직전에 닉네임과 능력치를 다시 검증한다.

### 5.3 DOM과 Canvas 책임

- 캐릭터와 월드, 투사체, 드롭, 포탈, HUD 그래픽은 Phaser Canvas로 렌더링한다.
- 입력창, 버튼, 링크, 설정 range, 긴 목록과 팝업은 접근 가능한 DOM으로 만든다.
- DOM 팝업이 열리면 월드 입력을 멈추고 Canvas를 비활성화하며, 닫으면 Canvas 포커스를 복구한다.
- 터치 장치의 이동 조이스틱과 액션 버튼은 접근 가능한 DOM으로 만들고 팝업·시네마틱 중 숨긴다.
- 터치 조이스틱은 좌우 이동과 아래 입력만 담당한다. 점프는 항상 고정 버튼으로 유지하고,
  NPC 근처의 `대화`와 포탈·줄 근처의 `이동`은 별도 버튼으로 표시한다. 유틸리티 조작은
  `대화/이동 → 줍기 → 점프 → 공격` 순서를 유지한다.
- 모바일 HUD와 단축키 편집에는 현재 직업의 대표 액티브를 최대 4개 표시한다. 첫 페이지에는
  PC 환경 최적화와 BGM 재생을 위한 매너모드 해제 안내를 두 줄로 표시하고 3초 뒤 숨긴다.
- 760px 이하 세로 화면의 모든 DOM 화면과 팝업은 safe-area 안에서 `100dvh` 모바일 시트가 된다.
  긴 내용은 수평 넘침 없이 패널 자체를 세로 스크롤하고 제목·닫기·확정 조작을 고정하며,
  입력 16px 이상과 버튼·링크 44px 이상의 터치 영역을 유지한다.
- 게임 상태는 `aria-label`, live region과 테스트 가능한 `data-*` 속성에도 동기화한다.

## 6. 월드·이동·입력 명세

### 6.1 이동 규칙

- 플레이어 상태는 `idle`, `walk`, `jump`, `fall`, `attack`, `skill`, `hurt`, `climb`으로 분리한다.
- 지상 좌우 가속·감속, 점프 상승·정점·낙하, 월드 경계와 지면·단방향 발판 충돌을 프레임 독립적으로 처리한다.
- 기본 수평 최고속도는 260, 구미호 변신 중에는 312다. 공중에서 좌우 입력이 있으면 해당 방향
  최고속도로 즉시 반전하고, 입력이 없으면 현재 수평 관성을 유지한다.
- 점프 입력은 공격보다 먼저 판정한다. 공중 공격은 현재 y 속도와 무입력 x 관성을 보존하면서도 좌우 조향을 계속 허용한다.
- `↓+Alt`는 플레이어 발이 겹친 현재 단방향 발판 하나만 통과하며 고정 지면에서는 점프하지 않는다.
- 줄 근처의 `↑/↓`로 붙고 이동하며 `←/→` 또는 `Alt`로 이탈한다. 사다리는 사용하지 않는다.
- 피격 시 950ms 무적, 80ms 간격 점멸, 넉백을 적용한다. HP 0이면 안전한 마을 위치로 귀환한다.
- 카메라는 월드 경계 안에서 플레이어를 추적하고 y=0 위로 올라가지 않는다. 하단 HUD를 피하도록 월드 화면을 48px 올린다.
- 게임 Canvas에 포커스가 있을 때만 브라우저 기본 단축키를 막는다.

### 6.2 조작표

| 키 | 동작 |
| --- | --- |
| `←` / `→` | 좌우 이동, 줄 이탈 |
| `↑` | 포탈·NPC 상호작용, 줄 올라가기 |
| `↓` | 줄 내려가기 |
| `Alt` | 점프, 줄 이탈 |
| `↓+Alt` | 현재 단방향 발판 하강 |
| `Ctrl` | 기본 표창, 변신 중 근접 할퀴기 |
| `1~0`, `-` | 사용자가 정렬한 11개 액티브 슬롯 |
| `Shift`, `QWER`, `ASDF`, `XCV` | 비우거나 중복 배치할 수 있는 추가 액티브 스킬 키 |
| `B/N` | 구미호 변신/삼인 협공 고정 보조키 |
| `Z` | 범위 안의 가장 가까운 드롭 회수(고정) |
| `S` | 추가 스킬이 없을 때 능력치·표창 장비 통합 창 |
| `K` | 스킬·SP 분배 창 |
| `I` | 24칸 인벤토리 |
| `Esc` | 전체 메뉴 열기, 현재 팝업·크레딧 닫기 |

숫자열과 숫자패드를 모두 지원한다. `Esc` 전체 메뉴에서 인벤토리, 능력치·표창 장비, 스킬,
전용 스킬 단축키와 환경설정을 연다. 전용 스킬 단축키 화면의 `1~0/-` 11칸은 액티브 스킬의
순열을 유지하고 드래그 앤 드롭이나 `Alt+←/→`로 교환한다. 추가 키 12개는 빈 값과 중복 배치를
허용한다. 기본 배치는 기존 `Shift/QWER/XCV` 별칭을 보존하며 `A/D/F/S`는 비어 있다. `Z`는
설정 화면에 표시하지 않고 항상 수동 회수에 사용하며, 우측 상단 조작 안내에 `Z 줍기`를 표시한다.
모든 스킬 키 변경은 즉시 HUD, 입력과 현재 캐릭터 슬롯에 반영한다.

터치 장치에서는 좌하단 조이스틱으로 좌우 이동하고 아래 입력을 유지한 채 고정 점프를 눌러
현재 단방향 발판을 하강한다. 우하단에는 고정 점프·공격·줍기를 유지하고, NPC 근처의 `대화`와
포탈·줄 근처의 `이동`을 별도 버튼으로 표시한다. 유틸리티 조작과 DOM 포커스 순서는
`대화/이동 → 줍기 → 점프 → 공격`이다. 같은 영역에는 현재 직업 차수의 대표 액티브 스킬을
최대 4개 표시한다. 로그는 럭키세븐·그림자 연사, 어쌔신은 드레인·환영 쌍성, 허밋은
어벤저·심연 폭우, 호카게는 나선환·미수옥·삼인 협공·천뢰옥이다. 모바일 스킬 단축키 화면도
같은 최대 4개를 표시하고 상대 순서를 저장한다. 우상단 `MENU`는 `Esc` 전체 메뉴와 같은 동작이다.

### 6.3 맵 구성

월드 크기, 배경 레이어, 발판, 줄, 포탈, 스폰, NPC, 몬스터와 장애물은 하나의 데이터 파일을
단일 기준으로 사용한다. 배경 픽셀을 충돌 판정에 사용하지 않는다.

| 맵 | 권장 레벨 | 월드 | 역할과 필수 구성 |
| --- | ---: | ---: | --- |
| 커닝시티 | 마을 | 1920×720 | 시작·회복·상점·원정·개발자 NPC, 아지트/동굴/서고 포탈 |
| 도적 아지트 | 전직 | 1280×720 | 전직 NPC, 마을 귀환, 활성 전직 시험 전용 포탈 |
| 초록버섯굴 | 10~29 | 1920×720 | 초록버섯 4체, 다단 발판, 도시·던전·인내의 숲 분기 |
| 그림자 시험장 | 전직 | 1920×720 | 기본 파수꾼 3체·골렘 1체, 1차 시험 중 초록버섯 4체로 치환, 단방향 발판과 줄 1개 |
| 수정 개미굴 | 30~49 | 1920×720 | 수정굴 늑대 4체, 발판 3개와 줄 1개 |
| 시계태엽 탑 | 50~69 | 1920×720 | 망각된 기술자 좀비 4체, 발판 3개와 줄 1개 |
| 가라앉은 산호 신전 | 70~99 | 1920×720 | 산호 맹그로브 4체, 발판 3개와 줄 1개 |
| 잿불 광산 | 100~139 | 1920×720 | 잿불 광부 좀비 4체, 중간보스 1체, 줄 1개 |
| 달빛 마도서고 | 140~199 | 1920×720 | 월광 늑대 4체, 상위보스 1체, 결투장·도시 포탈, 줄 1개 |
| 무한의 결투장 | 200 | 1920×720 | 연속 지면, 2단 단방향 발판 5개, 귀환 포탈, 최종보스 1체 |
| 인내의 숲 | 비전투 | 1920×1440 | 단방향 발판 20개, 줄 4개, 왕복 장애물 10개, 입구·정상 포탈 |

포탈은 문틀, 세로 에너지, 이중 바닥 광륜과 상승 파편을 분리해 반복 애니메이션한다. 모든 포탈
트윈, 보스 투사체, 드롭, 임시 이펙트와 타이머는 현재 맵 수명에 묶어 전환·Scene 종료 때 제거한다.
커닝시티의 달빛 서고 포탈은 원정이 `offer|midboss`일 때 오브젝트·효과·미니맵에서 함께 숨기고,
이그니카르 처치와 보상 확정으로 `upperboss`가 된 뒤에만 생성한다.

## 7. 성장·퀘스트·경제 명세

### 7.1 레벨과 능력치

- 최대 레벨은 200이다.
- 다음 레벨 필요 EXP는 `100 + max(0, floor(level) - 10) × 40`이다.
- 레벨마다 최대 HP +14, 최대 MP +7, AP +5, SP +3을 지급하고 HP/MP를 완전 회복한다.
- AP 자동분배는 5점마다 `LUK +4, DEX +1`이며 나머지는 LUK에 준다.
- 자연 회복은 기본 1600ms마다 HP 6, MP 4다. 피격 후 2600ms 동안 회복을 지연한다.
- `그림자 호흡`과 `선인모드`가 회복 주기를 곱연산으로 단축한다.
- 상점의 1메소 `경험의 서`는 한 권당 최대 10레벨을 올리되 Lv.200을 넘지 않는다.
- 회복 물약은 인벤토리에서 사용해 HP·MP를 각각 최대치의 50% 회복한다. 둘 다 가득 찼으면 소비하지 않는다.

### 7.2 전직 시험

| 현재 직업 → 다음 직업 | 필요 레벨 | 목표 | 처치 수 | 해금 |
| --- | ---: | --- | ---: | --- |
| 초보자 → 로그 | 10 | 초록버섯 | 5 | 1차 액티브 2, 패시브 1 |
| 로그 → 어쌔신 | 30 | 그림자 파수꾼 | 6 | 2차 액티브 2, 패시브 1 |
| 어쌔신 → 허밋 | 60 | 심연의 골렘 | 3 | 3차 액티브 2, 패시브 1 |
| 허밋 → 호카게 | 120 | 심연의 골렘 | 8 | 4차 액티브 5, 패시브 1 |

흐름은 항상 `필요 레벨 달성 → NPC에게 시험 수락 → 대상 처치 → 같은 NPC에게 완료 보고`다.
처치 진척은 사망 애니메이션과 보상 확정 뒤에만 증가한다. 퀘스트 HUD는 현재 레벨, 추천 사냥터,
수락, 처치 수와 보고 중 정확히 다음 행동을 최대 두 줄로 표시한다.

### 7.3 보스 원정과 인내의 숲

- 원정명은 `깨어난 던전 회랑`, 적정 레벨은 100~200이다.
- Lv.100부터 원정 NPC에게 수락하고 잿불 광산의 중간보스, Lv.140 달빛 마도서고의 상위보스,
  Lv.200 무한의 결투장 최종보스를 순서대로 처치한 뒤 보고한다.
- 원정 1회 보상은 7,500메소와 경험의 서 2권이다. 진행 단계를 건너뛸 수 없다.
- 인내의 숲 정상 최초 보상은 1,000,000메소와 상점에서 팔지 않는 6등급 `초대형 고드름`이다.
- 다음 목표 HUD는 Lv.100 미만에서 보스 안내를 숨기고, 이후 현재 원정 단계와 레벨에 맞는 한 줄만 표시한다.

### 7.4 장비와 인벤토리

| 등급 | 이름 | 가격 | 표창 피해 배율 | 비고 |
| ---: | --- | ---: | ---: | --- |
| 1 | 수련생 표창 | 50 | 1.00 | 기본 소유 |
| 2 | 강철 표창 | 250 | 1.08 | 상점 |
| 3 | 청옥 표창 | 1,000 | 1.18 | 상점 |
| 4 | 홍염 표창 | 4,000 | 1.32 | 상점 |
| 5 | 일식 표창 | 12,000 | 1.50 | 상점 |
| 6 | 초대형 고드름 | 판매 안 함 | 2.00 | 인내의 숲 최초 보상, 투사체 1.75배 |

표창 배율은 표창 계열 공격과 변신 전용 구미호 할퀴기에 적용하고, 그 밖의 차크라 계열에는
적용하지 않는다. 구매, 소유, 장착과
메소 차감은 하나의 원자적 결과로 계산해 즉시 저장한다. 인벤토리는 24칸이며 아이템 수량, 메소,
보유 표창과 현재 장착 상태를 실제 프로필에서 읽는다. 상세 패널의 접근 가능한 `사용하기` 버튼은
회복 물약과 경험의 서에만 표시하며 성공 직후 수량·HP/MP·EXP·레벨과 저장 상태를 함께 갱신한다.

서적상 레오는 50,000메소 `멍푸치노`와 1,000,000메소 `부활의 부적`도 판매한다. 멍푸치노는
보유 중이거나 두아가 이미 등록된 슬롯에서 중복 구매할 수 없고, 두아에게 줄 때만 1개를 소비한다.
부활의 부적은 슬롯마다 최대 1개만 보유하며 직접 사용할 수 없다. HP가 0이 되면 자동으로 1개를
소비해 같은 맵·위치에서 HP와 MP를 완전 회복하고, 부적이 없을 때만 720ms 뒤 커닝시티로 귀환한다.

### 7.5 자동 회수 펫 두아

- 커닝시티의 두아에게 멍푸치노를 주면 현재 슬롯의 펫으로 등록되고 맵 이동·새로고침 뒤에도 따라온다.
- 두아는 플레이어 주변 720px 안에서 바닥에 착지한 최근접 드롭을 선택하고 44px 안에서 자동 회수한다.
- 수동 `Z`와 자동 회수는 같은 보상 확정·저장 경로를 사용해 중복 지급을 막는다.
- 추적 최고속도는 360, 점프 속도는 700이다. 32px 이상 직상단 목표나 진행 방향 장애물을 감지해
  점프하고, 수평 560px 또는 수직 220px 이상 낙오하면 플레이어 뒤 추적 지점으로 이동한다.
- 플레이어가 움직이는 동안 추적 간격 안에서도 현재 속도를 최대 360까지 이어받아 순항하고,
  플레이어가 멈추면 지면 마찰로 감속한 뒤 대기 자세로 전환한다.

## 8. 전투 명세

### 8.1 공통 규칙

- 공격은 입력 판정, MP·직업·변신 조건 검사, 릴리스 지연, 투사체 생성, 명중, 피해, 사망과 보상 순서로 처리한다.
- 초보자/로그/어쌔신/허밋/호카게 직업 전투 배율은 `1 / 12 / 70 / 350 / 1,400`이다.
- 레벨 배율은 `1 + (clamp(level, 10, 200) - 10) / 55`다.
- 표창 스탯 보너스는 `floor((LUK × 2 + DEX) / 16)`이며 구미호 할퀴기도 같은 무기 보정을 받는다.
- 몬스터 방어 적용 피해는 `max(1, floor(raw × 1000 / (1000 + defense)))`다.
- 원펀맨은 실제 바디 위 132px까지 전투 피격 영역을 확장하며, 투사체와 구미호 할퀴기의
  전방 사거리가 이 영역에 닿으면 2층·상층에서도 명중한다. 다른 몬스터에는 확장하지 않는다.
- 예리한 시야의 표창 기본 수명은 900ms이고 스킬 레벨당 2% 늘어난다.
- 치명 투척은 레벨당 1%p, 최대 20% 확률로 표창 피해를 1.5배로 만든다. 테스트가 재현 가능하도록 결정적 판정을 사용한다.
- 드레인은 실제 적용 피해의 45%를 최대 HP 이내에서 회복한다.
- 구미호 변신은 매초 최대 MP의 1%를 소비하고 자연 MP 회복을 정지하며, MP가 고갈되면 자동
  해제한다. 할퀴기는 기본 공격의 두 배 속도와 기본 표창과 같은 수평 탐색 거리를 사용한다.
- 공격 중 재입력, 직업·MP·변신 조건 실패와 포탈 동시 입력에는 명확한 피드백을 준다.

### 8.2 액티브 스킬

모든 스킬은 최대 20레벨이다. 표의 피해는 스킬 레벨과 직업·레벨·장비 배율 전의 타격당 기본 범위다.

| 차수 | 스킬 | 기본/보조키 | 타수×최대 대상 | MP | 기본 피해 | 핵심 효과 |
| ---: | --- | --- | ---: | ---: | ---: | --- |
| 기본 | 기본 표창 | `Ctrl` | 1×1 | 0 | 24~32 | 현재 표창 투사체 |
| 1 | 럭키세븐 | `Shift` | 2×1 | 12 | 18~24 | 높이가 다른 평행 쌍투척 |
| 1 | 그림자 연사 | `Q` | 3×1 | 16 | 14~18 | 상·중·하 부채꼴 |
| 2 | 드레인 | `X` | 1×1 | 16 | 38~50 | 적용 피해 45% HP 회복 |
| 2 | 환영 쌍성 | `W` | 2×2 | 24 | 30~40 | 교차 파동, 각 투사체 관통 |
| 3 | 어벤저 | `C` | 1×4 | 24 | 34~44 | 대형 표창 다중 관통 |
| 3 | 심연 폭우 | `E` | 3×4 | 42 | 42~54 | 3회 심연 투척 |
| 4 | 나선환 | `V` | 1×1 | 30 | 72~90 | 근거리 차크라 폭발 |
| 4 | 구미호 변신 | `B` | 토글 | 활성 60 | - | 초당 최대 MP 1%, 자연 MP 회복 정지, 이동속도 +20% |
| 4 | 구미호 할퀴기 | 변신 중 `Ctrl` | 1×1 | 0 | 55~70 | 공격속도 2배, LUK/DEX·장착 표창 보정 |
| 4 | 미수옥 | 변신 중 `Shift` | 1×3 | 45 | 110~140 | 변신 전용 관통 탄환 |
| 4 | 삼인 협공 | `N` | 5×1 | 80 | 44~56 | 두 동료와 5타 시네마틱 |
| 4 | 천뢰옥 | `R` | 2×5 | 70 | 95~125 | 금빛 차크라 투사체 |

### 8.3 패시브 스킬

| 차수 | 스킬 | 레벨당 효과 | 최대 효과 |
| ---: | --- | --- | --- |
| 1 | 예리한 시야 | 표창 사거리 +2% | +40% |
| 2 | 치명 투척 | 치명타 확률 +1%p | 20%, 피해 150% |
| 3 | 그림자 호흡 | 회복 주기 -2% | -40% |
| 4 | 선인모드 | 회복 주기 -4% | -80% |

패시브는 단축키를 차지하지 않으며 SP 분배 즉시 적용한다. 액티브 아이콘은 `1~0/-` 11개,
패시브를 포함한 전체 전용 아이콘은 15개다.

### 8.4 몬스터와 보스

| 몬스터 | Lv. | HP | 방어 | 접촉 피해 | 역할 |
| --- | ---: | ---: | ---: | ---: | --- |
| 초록버섯 | 12 | 240 | 0 | 18 | 초반 사냥·1차 시험 |
| 그림자 파수꾼/수정굴 늑대 | 35 | 14,000 | 80 | 90 | 전직 시험/Lv.30 지역 |
| 망각된 기술자 좀비 | 50 | 50,000 | 140 | 150 | Lv.50 지역 |
| 심연의 골렘/산호 맹그로브 | 70/75 | 160,000/220,000 | 200/220 | 260/280 | 전직 시험/Lv.70 지역 |
| 잿불 광부 좀비 | 105 | 750,000 | 320 | 450 | Lv.100 지역 일반 몬스터 |
| 월광 늑대 | 150 | 4,000,000 | 450 | 850 | Lv.140 지역 일반 몬스터 |
| 폭열군주 이그니카르 | 100 | 4,000,000 | 400 | 600 | 중간보스 |
| 월식현자 루나시온 | 140 | 20,000,000 | 600 | 1,000 | 상위보스 |
| 원펀맨 | 200 | 80,000,000 | 400 | 즉사 | 최종보스 |

일반 사냥터 6종과 시험 몬스터를 포함한 11개 전투 항목은 모두 서로 다른 이동속도를 사용한다.
보스는 등급·이름·HP 게이지를 HUD에 표시한다. 이그니카르는 사거리 680의 650 피해 원거리 공격,
루나시온은 사거리 820의 1,600 피해 원거리 공격을 사용한다. 원펀맨은 넉백과 비치명 피격
경직·리코일을 무시하며, HP 50% 초과에서는 사거리 1,200의 보통 펀치 단일 즉사 충격파를,
50% 이하에서는 전개각 24도의 진심 펀치 3방향 즉사 충격파를 사용한다. 두 패턴은 전용 투명
SVG를 사용하고 표창 애니메이션과 분리한다. 투사체와 선딜 타이머는 명중, 만료, 보스 처치,
맵 정리와 Scene 종료에서 함께 제거한다.

### 8.5 드롭과 연출 순서

- 몬스터는 순찰, 접촉 공격, 피격, 사망 애니메이션, 보상, 재생성 상태를 공통 엔티티로 처리한다.
- 드롭은 위로 튀어 오른 뒤 발판에 착지하고 12초 뒤 사라진다.
- 첫 착지와 반동 착지에 작은 섬광을 주며 소멸 2.2초 전부터 140ms 간격으로 점멸한다.
- `Z`는 범위 안의 가장 가까운 드롭 하나를 선택한다. 보상은 즉시 저장하고 이미지는 240ms 동안 플레이어에게 흡수된다.
- 데미지 숫자는 공격, 강한 공격, 플레이어 피격과 치명타를 구별하는 4개 픽셀 팔레트로 표시한다.
  치명타는 확대 연출을 사용하고, 남은 HP보다 큰 오버킬도 실제 계산된 원피해를 표시한다.
- 전직·레벨업·포탈 효과는 입력 잠금, 링, 스파크, 배너, 화면 플래시와 카메라 강조 시간을 데이터로 관리한다.
- 레벨업 연출 중에도 이동과 전투를 허용한다. 4차 액티브 시네마틱 중에는 기존 투사체의 수명을
  정지하며, 타이머, 동료, 트윈과 physics pause는 중단·맵 이동 시 완전히 복구한다.

## 9. HUD·팝업·접근성 명세

### 9.1 플레이 HUD

- 좌상단 미니맵: 맵 이름, 권장 레벨, 비율 유지 축소 지형, 실시간 플레이어·NPC·포탈 마커와 범례.
- 좌측 퀘스트: 전직 최대 2줄과 보스 다음 목표 1줄. 현재 진척에 맞는 행동만 표시.
- 우상단 조작 안내: 핵심 키와 현재 직업의 해금 상태.
- 상단 중앙 보스: 등급, 긴 이름, 축약 HP 수치와 게이지를 겹치지 않게 배치.
- 하단 124px HUD: y=596부터 캐릭터, HP, MP, EXP, 11칸 액션 바, 메소·인벤토리, AP·SP를 독립 경계로 배치.
- 월드 수치는 만/억/조/경 단위로 축약할 수 있지만 `aria-label`에는 전체 정수를 유지한다.
- 미니맵·퀘스트·조작 창은 반투명 은회색 프레임, 청회색 제목띠와 독립 `−/+` 접기 버튼을 사용한다.
- 접힌 상태에도 제목과 버튼을 남기고 `aria-expanded`를 동기화한다. 버튼 중심은 제목 영역의 수직 중심과 일치해야 한다.

### 9.2 DOM 팝업

- `I`: 24칸 인벤토리.
- `S`: STR/DEX/INT/LUK 분배, 남은 AP, 자동분배 옵션, 표창 소유·장착을 한 모달에 표시.
- `K`: 4개 차수 탭, 액티브·패시브, 스킬 레벨/최대치, 효과, 남은 SP, 상단 슬롯 편집.
- `Esc`: 인벤토리·능력치/장비·스킬·전용 스킬 단축키·환경설정을 여는 전체 메뉴.
- NPC: 전직, 회복, 상점, 보스 원정과 선택형 개발자 안내 대화.
- 커닝시티 초반 동선의 개발자 NPC는 제작자가 설정한 표시 이름과 채널 URL을 데이터로 받고, 실제 `<a>`로 새 탭을 연 뒤 감사 동작만 재생한다. 구독·팔로우 완료를 추정하거나 프로필에 저장하지 않는다.
- 모든 팝업은 현재 16:9 게임 영역보다 커지지 않고 자체 세로 스크롤을 사용한다.
- 제목과 닫기 버튼은 프레임 안에 고정한다. 수평 스크롤은 허용하지 않는다.
- 1100px 이하에서 능력치·장비를 한 열로, 760px 이하에서 나머지 복수 열 콘텐츠를 한 열로 바꾼다.
- 사운드 조작은 상단 HUD에 두지 않고 전체 메뉴의 환경설정에서만 제공한다. 팝업이 열려 있거나
  호카게 시네마틱 중일 때 HUD 접기 버튼을 숨겨 겹침을 막는다.
- 링크는 실제 `<a>`를 사용하고 새 탭이면 `noopener noreferrer`를 적용한다. 외부 행동 완료 여부를 임의로 추정하지 않는다.

### 9.3 최종보스 크레딧

- 원펀맨의 사망 애니메이션, 보상, EXP와 원정 진척 저장이 확정된 뒤 18초 크레딧을 표시한다.
- 텍스트 없는 독자 픽셀 아트 배경 위에 시스템·던전·몬스터·캐릭터·스킬·FX·테스트를
  `임상진 with Codex`, 게임 레퍼런스를 `메이플스토리`로 접근 가능한 DOM 텍스트로 렌더링한다.
- 크레딧 직전 살아 있는 보스 투사체를 제거한다. 자동 종료, 계속 플레이 버튼과 `Esc`는 모두
  현재 런타임 플레이로 복귀시키고 Canvas 포커스와 입력을 복구한다.
- 크레딧 표시 여부는 프로필에 저장하지 않아 원펀맨을 다시 처치하면 다시 표시한다.

## 10. 저장 명세

프로필 스키마는 버전을 가진다. 현재 기준은 v16이며 신규 필드가 추가될 때 이전 버전의 유효한 값을
보존하고 누락·손상값만 안전한 기본값으로 정규화한다.

| 목적 | 키 |
| --- | --- |
| 1번 슬롯 | `kerning-shadows.local-profile.v1` |
| 2번 슬롯 | `kerning-shadows.local-profile.v1.slot-2` |
| 3번 슬롯 | `kerning-shadows.local-profile.v1.slot-3` |
| 현재 선택 | `kerning-shadows.local-profile.v1.active-slot` |
| 오디오 설정 | `kerning-shadows.local-settings.v1` |

각 캐릭터 슬롯은 닉네임, 직업, 레벨, HP/MP, EXP, STR/DEX/INT/LUK, AP·자동분배, 15개 스킬·SP,
11개 액티브 슬롯 순서, 12개 추가 키 별칭, 메소, 인벤토리, 표창 소유·장착, 전직 시험, 보스 원정,
두아 등록과 11개 맵 위치를 독립 저장한다. v15는 두아 등록을, v16은 추가 스킬 키를 저장한다.
v1~v14에는 미등록 두아를 보충하고 v1~v15에는 기존 `Shift/QWER/XCV` 고정 보조키를 기본
별칭으로 이관한다. v16 프로필에 남은 `Z` 별칭은 정규화할 때 제거한다. 오디오 설정은 캐릭터와
분리한다. 구미호 변신이나 크레딧 표시처럼 일시적인
런타임 상태는 저장하지 않는다.

저장 파서는 JSON 구조, 안전한 정수, 범위, enum과 상호 일관성을 모두 검사한다. 한 슬롯의 손상이
다른 슬롯을 초기화해서는 안 되며, 가득 찬 슬롯을 신규 생성으로 덮어쓰지 않는다.

## 11. 그래픽·스프라이트·맵 에셋 명세

### 11.1 스프라이트

- 핵심 런타임 시트는 24종이며 각 512×512 RGBA, 4×4 고정 그리드, 셀당 128×128으로 총 384셀이다.
- 플레이어는 `idle`, 4단계 지면 보행, `jump/fall`, `hurt`, 기본 공격과 스킬 공격을 표현한다.
- 두아의 달리기 4프레임은 `대각발 접지 → 회수 → 반대 대각발 접지 → 회수` 순서를 사용하며,
  좀비·늑대·고대 나무정령 시트는 기계·골렘형과 다른 실루엣과 바디를 사용한다.
- 각 16개 프레임의 시각 중심과 발 기준 보정값을 `sprite-manifest.json`의 `frameOrigins`에 기록한다.
- 물리 바디의 월드 중심과 발 기준선은 프레임 원점이 달라도 고정되어야 한다.
- 투명 모서리, 크로마 잔여, 셀 점유, 인접 셀 픽셀 연결과 지상 엔티티 기준선을 자동 검사한다.
- 공통 시트 계약을 우선하고 생성형 원본의 인접 셀 오염은 픽셀 제거만 수행한다. 임의 재색상으로 고치지 않는다.

### 11.2 UI와 맵

- 스킬 아이콘 15개는 각각 128×128 독립 이미지이며 Phaser HUD와 DOM `<img>`가 같은 URL 에셋을 사용한다.
- 9-slice 패널은 96×96, 고정 slice 16px로 책·나무·금속/HUD 테마를 만든다. 이미지 안에 텍스트와 아이콘을 넣지 않는다.
- 한글 픽셀 폰트는 라이선스와 함께 로컬 WOFF2로 번들링한다.
- 맵 배경은 1280×720 원경을 사용하고, 패럴랙스·충돌 정렬 레이어와 환경 오브젝트를 분리한다.
- 맵 오브젝트 아틀라스는 실제 불투명 연결 영역만 프레임으로 등록한다. 발판의 `surfaceY`, 줄 `top`, 포탈 하단 `y`를 충돌 좌표와 맞춘다.
- 배경·오브젝트 픽셀은 장식일 뿐 충돌의 단일 기준은 맵 데이터다.
- 참고 이미지와 생성 원본은 `dist/`에서 제외하고 Vite URL import로 필요한 런타임 파일만 번들링한다.
- 최종 크레딧 WebP에는 텍스트를 넣지 않고 실제 역할·이름·계속 플레이 조작은 DOM으로 분리한다.

## 12. BGM·SFX 제작과 삽입 가이드

### 12.1 필요한 오디오

최소 BGM은 보컬 없는 2곡이다.
현재 저장소의 사용자 제공 BGM은 원저작자와 배포 권리가 확인되지 않은 예시이므로 복제하거나 새
게임의 공개 배포본에 재사용하지 않고, 아래 계약에 맞는 곡을 새로 제작한다.

| 곡 | 권장 성격 | 적용 |
| --- | --- | --- |
| 일반곡 | 60~90초, 따뜻한 도시 밤·모험·가벼운 긴장, 오래 들어도 피로하지 않은 중간 템포 | 로그인, 생성, 선택, 일반 맵 |
| 보스곡 | 45~75초, 빠른 타악·저음 펄스·상승하는 긴장, 전투 판독을 방해하지 않는 고강도 | 살아 있는 중간·상위·최종 보스가 있을 때 |

SFX는 UI 확인, 점프, 투척, 명중, 플레이어 피격, 몬스터 사망, 드롭 착지·회수, 포탈, 회복,
전직과 레벨업의 12종을 만든다. SFX는 외부 샘플 없이 오실레이터·엔벌로프·고정 seed 노이즈로
절차 생성하면 권리와 재현성을 관리하기 쉽다.

### 12.2 직접 작곡하는 방법

1. 일반곡과 보스곡의 BPM, 조성, 8~16마디 핵심 루프와 악기 팔레트를 먼저 정한다.
2. 도입부만 큰 소리가 나거나 끝에 긴 리버브가 남지 않도록 루프 첫 박과 마지막 박을 함께 설계한다.
3. DAW에서 루프 경계 전후를 복사해 클릭, 박자 단절과 잔향 끊김이 없는지 확인한다.
4. 원본 프로젝트와 48,000Hz/24-bit stereo WAV 무손실 마스터를 보관하고 게임용 48,000Hz stereo MP3를 한 번만 내보낸다.
5. 마스터는 대략 -16 LUFS, true peak -1 dBTP 이하에서 시작해 두 곡의 체감 음량을 맞추고 최종 게인은 게임 코드에서 조정한다.
6. 다른 게임 음원의 멜로디, 편곡, 샘플이나 악기 스템을 가져오지 않는다.
7. 제작자, 제작일, DAW, 사용한 상업 샘플의 라이선스, 원본과 출력 파일을 `ASSET_CREDITS.md`에 기록한다.

### 12.3 Suno로 작곡하는 방법

Suno를 사용할 때는 Create의 Custom 모드에서 Instrumental을 켜고, 고유명이나 특정 작품·아티스트가
아닌 분위기, 템포, 악기, 구조와 게임 내 용도를 설명한다. 여러 결과를 만든 뒤 멜로디 독자성,
루프 경계, 과도한 고역과 보컬성 음색을 직접 검수한다.

일반곡 예시 프롬프트:

```text
Original instrumental background music for a 2D pixel-art side-scrolling fantasy city at night.
Warm plucked synth, soft chip-like lead, round bass, light breakbeat percussion, subtle industrial ambience,
126 BPM, adventurous and welcoming with a hint of mystery, clear 8-bar motif, loop-friendly intro and outro,
no vocals, no spoken words, no fade-in, no fade-out, no long intro, no final hit, no recognizable existing melody,
the final bar reconnects harmonically to the first bar, balanced for long gameplay sessions.
```

보스곡 예시 프롬프트:

```text
Original instrumental boss battle music for a fast 2D action RPG.
Driving toms, sharp electronic arpeggio, tense low strings, distorted but controlled bass, heroic counter-melody,
152 BPM, immediate momentum, controlled 8-bar intensity, clean loop-friendly ending, no vocals, no spoken words,
no fade-in, no fade-out, no long intro, no breakdown, no final hit, no recognizable existing melody,
the final bar reconnects harmonically to the first bar, leave space for combat sound effects.
```

생성 뒤 다음을 지킨다.

1. 곡을 만든 당시의 요금제, 생성일, 곡 ID/URL, 사용 프롬프트, 다운로드 원본 SHA-256, 편집 내역과 런타임 출력 SHA-256을 기록한다.
2. Suno 웹의 Library/Workspace에서 곡 메뉴의 Download로 파일을 내려받고 가능한 경우 무손실 원본을 보관한다.
3. 무료 플랜 결과를 나중에 유료로 가입했다는 이유만으로 상업 배포 가능하다고 가정하지 않는다. 무료 결과는 공개 비상업 게임이라도 현재 약관이 명시적으로 허용하는지 확인되지 않으면 사용하지 않는다.
4. Suno 공개 도움말은 유료 구독 중 생성한 곡에 상업적 이용 권한을 부여하고 무료 플랜
   생성물은 비상업 용도로 안내한다. 정책과 지역별 저작권 인정은 바뀔 수 있으므로 생성 직전과 배포
   직전에 [Suno 권리 안내](https://help.suno.com/en/articles/2416769),
   [상업적 이용 안내](https://help.suno.com/en/articles/9601985),
   [다운로드 안내](https://help.suno.com/en/articles/2409921)와
   [최신 이용약관](https://suno.com/terms)을 다시 확인한다. 유료 플랜의 이용 허용도 각 지역의
   저작권 성립이나 결과의 독자성·제3자 권리 비침해를 보증하지 않는다.
5. 권리가 불명확한 업로드 오디오, 다른 사용자의 곡 확장이나 재사용 프롬프트 결과는 배포본에 넣지 않는다.

### 12.4 게임에 삽입하는 방법

1. 파일을 `assets/audio/bgm/game-theme-v1.mp3`, `assets/audio/bgm/boss-theme-v1.mp3`라는 역할 기반 계약으로 둔다. 이름을 바꾸면 Vite import와 `audit-dist.mjs`의 BGM 예산 제외 정규식도 함께 갱신한다.
2. `audio-assets.ts`에서 Vite URL import, 고정 에셋 키, `category`, `baseVolume`과 `loopWindow`를 등록한다.
3. 파형 편집기에서 실제 첫 소리와 반복 직전의 끝을 초 단위로 찾아 `startSeconds`, `endSeconds`에 기록한다.
4. 다운로드 원본과 DAW 무손실 마스터는 그대로 보존하고, 루프 편집을 끝낸 마스터에서 런타임 MP3를 한 번만 만든다. Phaser 사운드 마커는 그 MP3에 남은 앞뒤 패딩만 재생에서 제외한다.
5. 로그인부터 일반 맵까지 같은 일반곡 키를 공유해 Scene/맵 이동에도 재생 객체와 위치를 유지한다.
6. 초기 오디오 목록에는 일반곡과 SFX만 넣는다. 보스 MP3는 살아 있는 보스를 처음 만났을 때
   동적으로 요청하고, 준비 중에는 일반곡의 재생 위치를 유지한다.
7. 보스곡 중복 요청은 한 Promise로 병합하고 성공 결과를 캐시한다. 실패는 다음 조우에서
   재시도하며, 맵을 떠나거나 보스를 처치한 뒤 끝난 늦은 요청은 전환하지 않는다.
8. 준비된 보스곡은 살아 있는 보스가 있을 때만 재생하고 사망 애니메이션·보상 확정 뒤 일반곡으로
   돌아간다. 보스 재생성 시 다시 보스곡을 시작한다.
9. 같은 키 재요청은 중복 사운드를 만들지 않고, 실제 키가 달라질 때만 기존 객체를 stop/destroy한다.
10. 브라우저 자동 재생 정책을 고려해 첫 사용자 입력 뒤 잠금을 해제하고 그 전에는 오류로 취급하지 않는다.
11. 전체 음소거와 BGM/SFX range를 제공한다. 신규 기본값은 BGM 45%, SFX 38%이며 캐릭터와 별도로 저장한다.
12. 첫 입력 전후, 화면 전환, 일반↔보스, 음소거, 볼륨 저장 복구, 루프 경계와 중복 재생 0건을 실제 스피커로 확인한다.

사운드 마커는 박자·화성 단절이나 MP3 경계 클릭을 자동으로 고치지 않는다. 최소 10회 연속 반복을
듣고 끊김이 있으면 DAW의 마디 경계와 짧은 크로스페이드를 먼저 고친 뒤 런타임 파일을 다시 만든다.

### 12.5 SFX 생성과 삽입 방법

1. 외부 샘플 없이 오실레이터·엔벌로프·고정 seed 노이즈로 12종 PCM을 만드는 스크립트를 작성하고 `npm run assets:audio`로 재생성한다.
2. 출력은 22,050Hz, 16-bit, mono WAV로 만들고 UI 확인, 점프, 투척, 명중, 플레이어 피격, 몬스터 사망, 드롭 착지·회수, 포탈, 회복, 전직, 레벨업 키에 등록한다.
3. 키 입력 순간이 아니라 실제 점프 시작, 투사체 릴리스·유효 명중, 피해·보상·전직·레벨업이 확정된 순간에 정확히 한 번 재생한다.
4. BGM과 SFX의 baseVolume을 분리하고 동시 타격에서도 클리핑이나 같은 사건의 중복 재생이 없는지 확인한다.
5. 생성 스크립트, 파일 해시, 적용 사건과 권리 상태를 `assets/audio/README.md`와 `ASSET_CREDITS.md`에 기록한다.

## 13. 권장 코드 구조와 단일 기준

```text
src/game/
  scenes/        Boot, Login, CharacterCreate, CharacterSelect, Gameplay
  assets/        Vite URL, 오디오·맵·UI·스킬 에셋, 매니페스트 검증
  input/         물리 키 → 게임 액션, 포커스 안전 처리
  data/          플레이어·몬스터·NPC·아이템 카탈로그
  maps/          맵 크기·발판·줄·포탈·스폰·장애물 데이터와 순수 규칙
  entities/      플레이어·몬스터·스프라이트 배치·등반 런타임
  combat/        공격·전직·방어·보스 원거리·데미지 숫자 순수 규칙
  progression/   EXP·레벨·회복·AP 규칙
  skills/        액티브·패시브·SP·슬롯 규칙
  inventory/     소모품 사용·부활의 부적 규칙
  equipment/     표창 카탈로그·구매·장착·배율
  pets/          두아 등록·추적·자동 회수 순수 규칙
  loot/          드롭·가까운 회수·표현 규칙
  quests/        전직·보스·인내의 숲 상태 머신
  profile/       생성 검증·v16 프로필·마이그레이션
  settings/      캐릭터와 분리된 오디오 설정
  effects/       포탈·전직·레벨업·공격·시네마틱 수치
  audio/         전역 BGM/SFX 수명주기
  ending/        원펀맨 크레딧 문구·길이·표시 조건
  ui/            DOM 오버레이·HUD·미니맵·bounds·테마
assets/
  sprites/       생성 원본, 런타임 시트, sprite-manifest.json
  maps/          배경, 레이어, 오브젝트 아틀라스
  ui/            화면, 패널, 폰트, 스킬 아이콘, 시네마틱
  audio/         직접 제작/Suno BGM, 절차 생성 SFX, 권리 문서
tests/e2e/       프로덕션 preview 대상 핵심 루프·뷰포트 테스트
scripts/         에셋 생성·정리·릴리스 고지·dist 감사
```

수치와 조건은 UI 문자열과 Scene 코드에 중복하지 않는다. 다음 항목은 각각 한 데이터/순수 규칙
모듈만 기준으로 삼는다.

- 맵과 월드: 맵 정의
- 공격, 스킬, 전직: 전투·스킬 규칙
- 몬스터: 몬스터 카탈로그
- 성장, 장비, 드롭, 퀘스트: 각 도메인의 순수 함수
- 펫: 등록 상태, 추적 목표와 바닥 드롭 자동 회수 규칙
- 인벤토리: 소모품 사용과 부활의 부적 구매·자동 소비 규칙
- 저장: 프로필 파서와 마이그레이션
- 엔딩: 원펀맨 크레딧 문구, 길이와 표시 조건
- HUD 위치와 안전 여백: HUD bounds 규칙
- 오디오 키, 게인, 루프 구간: 오디오 에셋 정의
- 포탈·전직·레벨업·시네마틱 시간: 효과 규칙

## 14. 한 번에 사용하는 마스터 프롬프트

아래 프롬프트와 이 문서의 기능 명세 전체를 함께 AI 코딩 에이전트에게 제공한다.

```text
당신은 TypeScript, Phaser 3, Vite, DOM 접근성, Vitest와 Playwright에 능숙한 시니어 게임 개발자다.
함께 제공된 “PC·모바일 횡스크롤 액션 RPG 제작 명세와 프롬프트”를 제품 요구사항의 단일 기준으로 삼아,
독자적인 픽셀 아트 로컬 싱글 플레이 웹 게임을 처음부터 구현하라.

핵심 결과:
- 1280×720 논리 해상도, PC 키보드·모바일 터치, 800×600 PC 창과 가로·세로 터치 화면에 안전하며 세로 화면의 Canvas 위아래 영역까지 UI에 사용하는 Phaser 3 횡스크롤 게임
- Boot → 임의 로컬 로그인 → 첫 캐릭터 생성 → 최대 3슬롯 선택 → Gameplay 흐름
- 11개 데이터 기반 맵, 지면·단방향 발판·줄·포탈·장애물·카메라와 맵 수명주기
- Lv.10~200 성장, 4회 전직 시험, 11개 액티브·4개 패시브, AP/SP와 6등급 표창 장비
- 서로 다른 속도·실루엣의 일반 몬스터와 중간·상위·2페이즈 최종 보스, 원거리 패턴, 드롭·인벤토리·상점·퀘스트
- 자동 회수 펫 두아, 자동 부활 부적과 원펀맨 처치 뒤 18초 픽셀 아트 크레딧
- 반투명 접이식 HUD, 축소 미니맵, 접근 가능한 DOM 팝업, 4개 PC 뷰포트와 가로·세로 터치 화면 대응
- 숫자/HUD 11칸과 `Shift/QWER/ASDF/XCV` 12칸을 편집하는 전용 스킬 단축키 화면, 고정 `Z` 회수
- v16 형태의 검증·마이그레이션 가능한 3슬롯 localStorage, 두아·추가 키와 별도 오디오 설정
- 직접 제작 또는 Suno로 권리를 확인해 만든 일반/보스 BGM, 절차 생성 SFX 12종, 보스곡 지연 로딩·루프·중복 방지
- 독자 에셋, 출처·라이선스 문서, 3엔진 E2E와 배포 산출물 감사

저작권과 독자성:
- 특정 상용 게임의 코드, 이미지, 음원, 맵, UI, 로고, 고유 문양·캐릭터를 복제하지 마라.
- 참고 자료는 화면 구성과 동작 분석에만 쓰고 아트, 명칭과 음악은 독자적으로 제작하라.
- 생성형 도구에 특정 작품·작가·현존 아티스트 모방을 요청하지 마라.
- 권리가 확인되지 않은 에셋은 런타임과 dist에 넣지 마라.

작업 방식:
1. 기존 저장소가 있으면 AGENTS.md와 사용자 변경사항을 먼저 확인하고 덮어쓰지 마라.
2. 구현 전에 에셋 감사표와 단계별 ROADMAP.md를 작성하라.
3. 에셋 계약과 데이터/순수 규칙을 Scene 런타임보다 먼저 작성하라.
4. 기능은 ‘입력 → 상태 변화 → 렌더링 → 저장 → 복구’ 수직 슬라이스로 구현하라.
5. 서로 독립적인 규칙은 병렬로 진행하되 같은 파일의 충돌을 피하라.
6. 숫자와 조건을 UI와 Scene에 중복하지 말고 도메인별 단일 기준 모듈을 두어라.
7. 모든 임시 객체, tween, timer, collider, projectile과 physics pause를 맵/Scene 수명에 묶어라.
8. 화면 입력은 접근 가능한 DOM, 월드는 Phaser로 만들고 팝업 닫힘 뒤 Canvas 포커스를 복구하라.
9. 각 단계마다 unit test, typecheck, build, 관련 Chromium/Firefox/WebKit E2E를 통과시켜라.
10. 큰 단계가 끝나면 README.md, ROADMAP.md, AGENTS.md, QA·에셋 문서를 갱신하고 의미 있는 커밋을 만들어라.

구현 순서:
P0 에셋 계약·프로젝트 기반 → P1 화면 흐름·3슬롯 저장 → P2 이동·충돌·맵·포탈 →
P3 전직·전투·드롭 → P4 성장·스킬·장비·UI → P5 던전·보스·퀘스트 →
P6 인내의 숲·맵 오브젝트 → P7 HUD·접근성·반응형 → P8 그래픽·연출·오디오 → P9 릴리스 QA.

각 단계 시작 시 변경할 파일과 완료 조건을 짧게 제시한 다음 즉시 구현하라. 불명확하지만 안전하게
추론 가능한 항목은 명세와 기존 코드 패턴에 맞춰 결정하고, 범위를 크게 바꾸는 선택만 질문하라.
완료했다고 말하기 전에 실행한 검증 명령과 결과, 남은 수동 검증, 변경 파일을 직접 확인하라.
```

## 15. 단계별 실행 프롬프트

마스터 프롬프트 이후 아래 문단을 한 단계씩 전달한다. 각 단계는 앞 단계의 테스트가 통과하고 커밋된
뒤 시작한다.

### P0 — 기반과 에셋 계약

```text
P0를 구현하라. TypeScript/Vite/Phaser 3 프로젝트와 Boot·Login·CharacterCreate·CharacterSelect·Gameplay
Scene 뼈대, Canvas 포커스 입력 계층, 1280×720 FIT 설정을 만든다. 동시에 ASSET_GUIDE.md에 4×4
스프라이트, 9-slice, 맵, 아이콘, 오디오 계약과 출처 상태를 기록한다. 임시 도형으로라도 로그인부터
Gameplay까지 실행되게 하고 typecheck, unit smoke test, build를 통과시킨 뒤 문서와 커밋을 갱신하라.
```

### P1 — 캐릭터 생성·3슬롯 저장

```text
P1을 구현하라. 비어 있지 않은 임의 아이디/비밀번호 로그인, 비밀번호 미저장, 첫 캐릭터 생성 강제,
2~12자 닉네임, 각 4~13·합계 25 주사위 스탯, 최대 3개 독립 슬롯과 선택 복구를 구현한다.
슬롯별로 Lv.9 초보자 또는 자동 능력치·전체 스킬 Lv.20·남은 SP 33의 Lv.120 호카게 부스트를
선택하되 다른 슬롯에는 영향을 주지 않는다.
생성 전 저장 키 미작성, 손상 슬롯 격리, v1부터 확장 가능한 버전 저장 파서와 접근 가능한 DOM 폼을
단위 테스트하고 새로고침 E2E로 검증하라.
```

### P2 — 이동·11개 맵·포탈

```text
P2를 구현하라. 가속 이동, 점프, 낙하, 지면·단방향 발판, ↓+Alt 하강, 줄 등반, 공중 공격 속도 보존,
경계 카메라와 11개 데이터 기반 맵을 만든다. 포탈 상호작용, 접근 조건, 저장 위치, 맵 전환 입력 잠금과
모든 임시 객체 정리를 구현한다. 배경 픽셀과 충돌 데이터를 분리하고 맵 정의·등반·하강·장애물 순수
규칙을 단위 테스트하며 실제 왕복 E2E를 추가하라. 달빛 서고 포탈은 이그니카르 처치 전까지
오브젝트·효과·미니맵에서 숨기고, 무한의 결투장에는 줄 없이 원펀맨만 배치하라.
```

### P3 — 전직·전투·몬스터·드롭

```text
P3를 구현하라. Lv.10/30/60/120 전직 시험의 수락·처치·보고 상태 머신, 직업 외형, 기본 표창과
차수별 전투를 순수 규칙으로 만든다. 일반 몬스터의 순찰·접촉·피격·사망·재생성, 방어, 무적, 데미지
숫자, 드롭·12초 소멸·Z 근거리 회수와 사망 후 보상 순서를 연결한다. 전직 전후 차단, MP, 타수,
실제 적용 피해, 진척·보상 확정 순서를 단위/E2E 테스트하라. 네 시험은 모두 그림자 시험장 입구를
사용하고 1차 시험 중에는 기존 4개 스폰을 초록버섯으로 바꿔 5체 처치 목표를 지원하라.
```

### P4 — 성장·스킬·장비·상점

```text
P4를 구현하라. Lv.200 상한, EXP 공식, 레벨당 HP/MP·AP5·SP3, LUK4/DEX1 자동분배, 빠른 자연
회복을 만든다. 액티브 11·패시브 4의 최대 20레벨, 1~0/- 교환 슬롯, 빈 값·중복을 허용하는
Shift/QWER/ASDF/XCV 추가 키와 고정 Z 회수, 6등급 표창, 1메소 경험의 서와 24칸 인벤토리를 추가한다.
인벤토리에서 회복 물약은 HP·MP 최대치의 50%씩, 경험의 서는 최대 10레벨 성장 효과로 사용하고
성공했을 때만 해당 소모품 1개를 차감한다. 완전 회복 상태에서는 물약을 소비하지 않는다.
50,000메소 멍푸치노·두아 등록과 자동 회수, 최대 1개의 1,000,000메소
부활의 부적·사망 즉시 자동 소비도 같은 슬롯 저장에 연결한다. ESC/K/S/I 접근 가능한 DOM 팝업과
사용 직후 상태·저장·손상값 정규화를 검증하라.
```

### P5 — 던전·보스·원정

```text
P5를 구현하라. Lv.30~200 던전 회랑, 늑대·좀비·나무정령 계열 일반 몬스터, 중간·상위·최종
보스, 보스 HP HUD와 원거리 패턴을 추가한다. 원펀맨은 넉백·비치명 경직을 무시하고 HP 50%를
경계로 단일 보통 펀치에서 3방향 진심 펀치 충격파로 전환한다. Lv.100 수락 →
Lv.100/140/200 순차 처치 → 보고 원정과 1회 보상을 상태 머신으로 만들고 단계를 건너뛰지 못하게
한다. 원펀맨 보상 확정 뒤 18초 크레딧을 열고 자동 종료·버튼·ESC 뒤 플레이를 재개한다.
보스 사망·보상 뒤 진척, 투사체 정리와 재생성, 즉사 패턴과 크레딧 복귀를 집중 테스트하라.
```

### P6 — 인내의 숲·환경 오브젝트

```text
P6를 구현하라. 1920×1440 인내의 숲에 단방향 발판 20, 줄 4, 결정적 왕복 장애물 10, 입구·정상
포탈을 배치한다. 정상 최초 보상 1,000,000메소와 6등급 표창을 중복 지급하지 않는다. 11개 맵의
환경 오브젝트 아틀라스를 실제 불투명 영역으로 등록하고 surfaceY/top/portal y를 충돌 좌표에 맞춘다.
```

### P7 — HUD·접근성·반응형 UI

```text
P7을 구현하라. 반투명 미니맵·퀘스트·조작 창과 독립 접기 버튼, 보스 HUD, y=596 하단 124px HUD,
11칸 액션 바를 만든다. 미니맵에 축소 지형과 실시간 플레이어·NPC·포탈 마커를 표시한다. 모든 DOM
팝업을 1280×720, 1600×900, 1024×768, 800×600에서 게임 영역 안에 제한하고 자체 스크롤,
고정 제목·닫기, aria 상태와 Canvas 포커스 복구를 검증하라. ESC 전체 메뉴에 기능 진입점과
음소거·BGM/SFX 환경설정을 배치하고 팝업·호카게 시네마틱 중 HUD 접기 버튼을 숨겨라.
가로 844×390·세로 390×844 터치 화면에는 좌우·아래 조이스틱, 고정 점프·줍기·공격,
별도 대화/이동, 대표 스킬 최대 4개와 MENU를 safe-area 안에 배치한다. 760px 이하 세로
DOM 화면은 Canvas 위아래까지 `100dvh`를 사용하고 수평 넘침 없이 자체 스크롤하게 하라.
```

### P8 — 아트·연출·BGM/SFX

```text
P8을 구현하라. 독자적인 4×4 스프라이트 24종·384셀과 15개 128×128 스킬 아이콘, 96×96 9-slice, 맵 배경과
오브젝트를 제작·최적화하고 프레임 원점·셀 오염 감사를 추가한다. 기준 PNG에서 픽셀 동일 lossless
WebP 런타임을 결정적으로 생성하고 배포물에는 WebP만 포함한다. 일반/보스 BGM은 직접 작곡하거나
Suno Custom Instrumental로 제작하되 생성 당시 권리와 프롬프트를 기록한다. 실제 첫 소리와 끝을
loopWindow로 정의하고 같은 키 연속 재생, 최초 보스 조우 전 보스 MP3 미요청, 조우 시 단일 지연
로딩과 준비 중 일반곡 유지, 보스 전환, 자동 재생 잠금, 음소거·BGM/SFX 저장을
구현한다. SFX 12종은 재생성 가능한 절차 PCM으로 만들고 실제 사건 확정 시점에 연결한다.
```

### P9 — 릴리스 QA

```text
P9를 구현하라. 핵심 순수 규칙 단위 테스트, Chromium·Firefox·WebKit에서 로그인부터 전직·전투·회수·
귀환·저장 복구 E2E, 4개 PC 뷰포트 경계, 맵 전환 객체 수명주기와 콘솔·pageerror·requestfailed 0건을
검증한다. 가로·세로 모바일 조작·팝업 경계·4슬롯 순서 저장과 WebP·보스 BGM 요청 시점도 3엔진에서
검증한다. dist에 고지와 크레딧을 포함하고 참고·생성 원본·소스맵·기준 PNG를 제외한다. BGM을 제외한 총량
8,000,000 bytes, JS 1,500,000 bytes, JS gzip 450,000 bytes를 감사한다. 실제 지원 브라우저와 30분
성능·스피커 QA의 결과 또는 미완료 상태를 RELEASE_QA.md에 사실대로 기록하고 최종 커밋하라.
```

## 16. 최종 인수 시나리오

1. 깨끗한 origin에서 네 프로필 키가 비어 있는지 확인한다.
2. 임의의 아이디/비밀번호로 로그인하고 첫 캐릭터 생성으로 이동한다.
3. 잘못된 닉네임과 스탯을 거부하고, 정상 생성 뒤 v16 프로필이 1번 슬롯에만 생기는지 확인한다.
4. 2·3번 캐릭터를 추가하고 한 슬롯은 Lv.120 호카게 부스트로 생성해 슬롯별 닉네임·성장·
   위치와 부스트 여부가 독립적인지 확인한다.
5. 커닝시티에서 HUD, 미니맵 마커, NPC, 포탈, 이름표와 Canvas 포커스를 확인한다.
6. 1차 시험을 수락하고 초록버섯 5체를 처치한 뒤 보고해 로그로 전직한다.
7. 기본 표창·럭키세븐의 타수, MP, 데미지 숫자, 드롭·Z 회수와 저장을 확인한다. 이어서 50,000 메소
   멍푸치노를 사 두아에게 주고, 등록·추적·바닥 드롭 자동 회수와 새로고침 복구를 확인한다.
8. 인벤토리에서 회복 물약과 경험의 서를 사용해 회복·수량 저장과 Lv.30·60·120 전직의 레벨, 수락, 처치와 보고를 차례로 검증한다.
9. AP/SP를 분배하고 액티브 슬롯을 바꾼 뒤 새로고침해 능력치·스킬·슬롯이 복구되는지 확인한다.
   `Shift/QWER/ASDF/XCV`에는 중복·빈 스킬을 배치하고, 빈 `S`는 기존 창으로 복귀하며 `Z`는
   설정과 관계없이 항상 회수 동작을 사용하는지 확인한다.
10. 표창 5등급을 구매·장착해 외형과 표창 계열 피해만 바뀌는지 확인한다.
11. 던전 회랑의 레벨별 몬스터와 줄, 발판, 포탈을 지나며 맵 위치 저장을 확인한다.
12. Lv.100 원정을 수락하고 중간·상위·최종 보스를 순서대로 처치해 보상과 BGM 전환을 확인한다.
    최종보스가 HP 50%에서 단일→3방향 충격파로 바뀌고 2층의 투사체·구미호 할퀴기에 맞는지,
    처치 뒤 픽셀 아트 게임 크레딧이 표시되고 자동 종료·버튼·ESC 뒤 플레이가 재개되는지 확인한다.
13. 인내의 숲 정상에 도달해 1회 보상을 받고 다시 도달해도 중복 지급되지 않는지 확인한다.
14. ESC 전체 메뉴, I/S/K와 모든 NPC 팝업을 4개 창 크기에서 열어 수평 넘침 0, 내부 스크롤,
    닫기와 포커스를 확인한다.
15. 로그인·일반 맵에서는 보스 MP3 요청이 없고 첫 살아 있는 보스 조우에서 정확히 한 번
    요청되는지 확인한다. 로딩 중 일반곡이 끊기지 않고 보스곡 전환·복귀와 루프 사이 무음이 없는지 듣는다.
16. 부활의 부적 보유 상태로 사망해 같은 위치에서 완전 회복하고, 다음 사망은 커닝시티로
    귀환하는지 확인한다.
17. 다른 슬롯 왕복과 브라우저 새로고침 뒤 모든 슬롯별 상태와 공통 오디오 설정을 확인한다.
18. 844×390과 390×844 터치 화면에서 `대화/이동 → 줍기 → 점프 → 공격`, 아래+점프 하강,
    대표 스킬 최대 4개 순서 편집, `100dvh` 팝업 스크롤과 safe-area를 확인한다.
19. 핵심 스프라이트 24종·스킬 아이콘 15종의 `currentSrc`가 WebP이며 `dist/`에 PNG가 없는지 확인한다.

## 17. 자동화와 릴리스 체크리스트

### 매 단계

- [ ] 새 데이터와 순수 규칙에 경계값·손상값 단위 테스트가 있다.
- [ ] `npm run typecheck`가 통과한다.
- [ ] `npm test`가 통과한다.
- [ ] `npm run build`가 통과한다.
- [ ] 화면·입력 변경이면 Gameplay Scene과 Canvas 포커스를 확인한 E2E가 있다.
- [ ] 맵·이펙트 변경이면 전환 뒤 sprite/timer/tween/collider/projectile 수가 기준으로 돌아온다.
- [ ] 사용자 변경사항을 덮어쓰지 않았는지 `git diff`와 `git status`를 확인한다.
- [ ] 큰 단계면 AGENTS.md, README.md, ROADMAP.md와 관련 QA·에셋 문서를 갱신한다.
- [ ] 완료 조건을 만족한 변경만 의미가 분명한 커밋으로 남긴다.

### 최종 릴리스

- [ ] Chromium·Firefox·WebKit의 핵심 루프와 저장 복구가 각각 통과한다.
- [ ] 1280×720, 1600×900, 1024×768, 800×600에서 HUD·DOM 경계 위반이 없다.
- [ ] 844×390·390×844 터치 화면에서 조작부·4슬롯·세로 DOM 시트가 safe-area와 화면 경계를 지킨다.
- [ ] 브라우저 console error, pageerror, requestfailed가 0건이다.
- [ ] 첫 입력 전후 오디오, 일반↔보스, 음소거·볼륨 복구와 실제 스피커 출력이 정상이다.
- [ ] `dist/`에 `ASSET_CREDITS.md`, 제3자 고지, 폰트·엔진 라이선스가 있다.
- [ ] 참고 이미지, 생성 원본, 프롬프트, 소스맵과 사용하지 않는 에셋이 `dist/`에 없다.
- [ ] 핵심 시트 24종·스킬 아이콘 15종은 기준 PNG와 픽셀 동일한 WebP이며 `dist/`에 PNG가 없다.
- [ ] BGM 제외 총량 8MB, JS 1.5MB, JS gzip 450KB 상한을 통과한다.
- [ ] 실제 Windows/macOS 지원 브라우저와 30분 성능 검증 상태를 사실대로 기록한다.

## 18. 흔한 실패와 방지법

- 배경 그림을 충돌체로 사용함: 맵 데이터 좌표만 물리의 단일 기준으로 사용한다.
- 공격 입력이 점프를 취소함: 점프를 먼저 판정하고 공중 공격의 y 속도·무입력 x 관성을 보존하며 좌우 조향은 계속 허용한다.
- 몬스터 사망 직전에 퀘스트가 오름: 사망 애니메이션과 보상 확정 뒤에만 진척을 반영한다.
- Scene 이동 뒤 보이지 않는 객체가 남음: 생성 즉시 맵 수명 컬렉션에 등록하고 한 정리 경로에서 제거한다.
- UI가 1280×720에서만 맞음: 네 PC 창 크기를 자동화하고 팝업 자체 스크롤을 사용한다.
- 저장 마이그레이션이 유효한 값을 잃음: 버전별 입력을 정규화하되 존재하는 성장·장비·퀘스트 값을 보존한다.
- 생성형 에셋이 셀 경계를 침범함: 런타임 최적화 전에 16셀 알파·연결·발 기준선을 자동 감사한다.
- BGM이 Scene마다 재시작됨: 화면이 아니라 에셋 키를 기준으로 전역 오디오 객체를 유지한다.
- 보스 BGM을 첫 화면에서 받음: 초기 에셋 목록에서 제외하고 최초 보스 조우의 단일 동적 요청으로만 불러온다.
- BGM 시작·반복에 공백이 남음: 원본을 훼손하지 말고 측정한 loopWindow 사운드 마커를 쓴다.
- Suno 결과의 권리를 추정함: 생성 당시 플랜과 약관을 기록하고 공개·상업 배포 직전에 다시 확인한다.
- 참고 작품과 너무 비슷해짐: 기능적 리듬만 남기고 고유명, 실루엣, 색 조합, 문양, 멜로디와 맵 지형을 독자화한다.
