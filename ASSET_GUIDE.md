# ASSET_GUIDE

## 에셋 감사표

> 목적: 구현 전에 에셋 계약과 출처 상태를 먼저 고정한다(SPEC §14 작업 방식 2, §15 P0).
>
> 상태 규칙: P0의 모든 행은 `미제작 (임시 도형 대체)`로 유지한다.

| 에셋/계약 | 수량·크기·포맷 | 적용 시점(단계) | 현재 상태 | 출처·권리 요구 |
| --- | --- | --- | --- | --- |
| 핵심 런타임 스프라이트 시트 | 24종, 각 512×512 RGBA, 4×4 고정 그리드, 셀 128×128, 총 384셀 (SPEC §11.1) | P8 | 미제작 (임시 도형 대체) | 직접 제작 또는 배포 권리 확인분만 사용. 생성 기록(요금제/생성일/프롬프트/SHA-256) 보존. 원본과 파생본을 구분하고 `dist/`에는 최적화한 파생본만 포함. 특정 작품·캐릭터·작가 모방 지시 금지. |
| `sprite-manifest.json` 프레임 원점 계약 | 24시트 × 16프레임의 `frameOrigins`, 시각 중심·발 기준 보정값 기록 (SPEC §11.1) | P8 | 미제작 (임시 도형 대체) | 런타임 시트와 1:1 대응. 생성형 원본 오염은 픽셀 제거만 허용, 임의 재색상 금지. 검수 기록과 해시 보존. |
| 스킬 아이콘 | 15종, 각 128×128 독립 이미지, Phaser HUD와 DOM `<img>`가 동일 URL 사용 (SPEC §11.2, §8.3) | P8 | 미제작 (임시 도형 대체) | 직접 제작 또는 권리 확인분만 사용. 제작자·원본·변환·라이선스를 `ASSET_CREDITS.md`에 추가. `dist/`에는 파생본만 포함. |
| 9-slice 패널 테마 | 테마별 96×96, slice 16px 고정, 책·나무·금속/HUD 테마, 텍스트·아이콘 이미지 내 삽입 금지 (SPEC §11.2) | P7~P8 | 미제작 (임시 도형 대체) | 참고 이미지는 분석용으로만 사용. 최종 배포물은 독자 제작본 또는 권리 확인본만 포함. |
| 한글 픽셀 폰트 | 로컬 WOFF2, 라이선스 동봉 필수 (SPEC §11.2) | P7~P8 | 미제작 (임시 도형 대체) | 폰트 제작자·라이선스·배포 조건 기록. `dist/`에는 런타임용 WOFF2와 필수 라이선스만 포함. |
| 맵 배경 원경 | 맵별 1280×720 원경, 패럴랙스/충돌 정렬 레이어/환경 오브젝트 분리 (SPEC §11.2, §6.3) | P6~P8 | 미제작 (임시 도형 대체) | 참고 이미지와 생성 원본은 `dist/` 제외. 런타임 파일만 Vite URL import로 번들링. 독자 지형·색 조합 유지. |
| 맵 오브젝트 아틀라스 | 실제 불투명 연결 영역만 프레임 등록, `surfaceY`/`top`/포탈 하단 `y`를 충돌 좌표와 정렬 (SPEC §11.2, §6.3, §15 P6) | P6~P8 | 미제작 (임시 도형 대체) | 원본과 파생본 구분. 충돌 단일 기준은 맵 데이터이며 장식 픽셀은 권리 문서와 함께 관리. |
| 일반 BGM | `assets/audio/bgm/game-theme-v1.mp3`, 보컬 없는 1곡, 60~90초, `loopWindow(startSeconds/endSeconds)` 필요 (SPEC §12.1, §12.4) | P8 | 미제작 (임시 도형 대체) | 직접 작곡 또는 Suno Custom Instrumental. 생성 기록(요금제/생성일/프롬프트/SHA-256, 곡 ID/URL, 편집 내역, 런타임 출력 SHA-256) 보존. Suno는 유료 플랜 생성분만 상업 검토, 무료 결과는 권리 명시 확인 전 배포 금지. `dist/`에는 런타임 MP3만 포함. |
| 보스 BGM | `assets/audio/bgm/boss-theme-v1.mp3`, 보컬 없는 1곡, 45~75초, `loopWindow(startSeconds/endSeconds)` 필요 (SPEC §12.1, §12.4) | P8 | 미제작 (임시 도형 대체) | 일반 BGM과 동일한 권리 기록 적용. 살아 있는 보스 맵에서 첫 요청, 지연 로딩·중복 방지·늦은 요청 무시·일반곡 위치 유지 계약을 준수한다(SPEC §12.4 6~9). |
| 절차 생성 SFX | 12종, 22,050Hz / 16-bit / mono WAV, `npm run assets:audio` 재생성 (SPEC §12.1, §12.5) | P8 | 미제작 (임시 도형 대체) | 외부 샘플 없이 오실레이터·엔벌로프·고정 seed 노이즈만 사용. 생성 스크립트·파일 해시·적용 사건·권리 상태를 기록. `dist/`에는 런타임 WAV만 포함. |
| 최종 크레딧 배경 | 텍스트 없는 독자 픽셀 아트 WebP 1종 (SPEC §11.2 말미, §9.3) | P8 | 미제작 (임시 도형 대체) | 역할·이름·조작 텍스트는 DOM으로 분리. 배경 이미지에는 문구 삽입 금지. 제작자·원본·변환 기록 필수. |

## 오디오 계약 메모

| 항목 | 계약 |
| --- | --- |
| 초기 로딩 | 일반곡과 SFX만 초기 목록에 포함, 보스 MP3는 최초 보스 조우 시 동적 요청 (SPEC §12.4-6) |
| 중복 방지 | 보스곡 중복 요청은 한 Promise로 병합, 성공 결과 캐시, 같은 키 재요청은 중복 사운드 생성 금지 (SPEC §12.4-7, §12.4-9) |
| 실패 처리 | 실패는 다음 조우에서 재시도, 맵 이탈·보스 처치 뒤 늦게 끝난 요청은 전환 금지 (SPEC §12.4-7) |
| 수동 검수 | 실제 스피커로 일반↔보스 전환, 루프 경계, 음소거·볼륨 복구, 중복 재생 0건 확인 (SPEC §12.4-12) |

## 기록 원칙

1. 제작 완료 행마다 `ASSET_CREDITS.md`에 같은 파일명을 기준으로 권리 정보를 추가한다(SPEC §4).
2. 참고 이미지, 생성 원본, 프롬프트, 기준 PNG는 런타임과 `dist/`에 포함하지 않는다(SPEC §4, §11.2, §17).
3. 문서에 없는 수량·크기·예산은 추가하지 않는다. 신규 계약이 생기면 먼저 `docs/SPEC.md`를 갱신한 뒤 본 문서를 맞춘다.
